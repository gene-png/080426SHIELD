# Golden output: the OLD rules, recorded once from `main`

Gene's condition on #620 (2026-09-25): a released assessment keeps rendering
what was delivered. These files are what `main` rendered. The test
`tests/unit/test_attack_parent_rules_golden.py` compares #620's output against
them. The files are **never regenerated by a test** (Gene's addition 2).

- **Recorded from:** `main` at `d64956695a308d08135676d03e3753fcf4609d0f`, on
  2026-09-25.
- **Recorded by:** `../record_parent_rules_golden.py`. It ran in a detached
  worktree at that SHA, in `shield-v2-api:latest`. How to re-derive it by hand
  is in that script's docstring.

| file                 | what `main` rendered                                                                 |
| -------------------- | ------------------------------------------------------------------------------------ |
| `world.sql`          | the database after the world was built, at `main`'s migration head (0052)            |
| `dashboard.json`     | the client dashboard of the RELEASED assessment, byte for byte                       |
| `finalize_b.json`    | finalizing the APPROVED, unreleased assessment: its summary text and every XLSX cell |
| `risk_findings.json` | the ATT&CK findings Risk Register synthesis sent                                     |

**No normalisation is needed.** The test loads `world.sql`, so ids and stored
timestamps are the ones recorded. The dashboard is compared byte for byte. The
finalize output carries no date, and none was found in it when it was recorded.
