{
 "incident": "#483: E2E_PERF and E2E_OIDC gate test.skip() in two specs, one a smoke spec, and no workflow sets either, so both have self-skipped on every CI run. A gate variable no workflow sets, unexempted, must be a finding naming the variable.",
 "expect": 1,
 "adversarial": true,
 "argv": [
  "--root",
  "{dir}"
 ],
 "stdout_contains": "E2E_PERF: read by"
}
