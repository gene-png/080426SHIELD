on: push
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - run: |
          rc=0
          python scripts/check_x.py > out.txt || rc=$?
          [ "$rc" -eq 0 ] || exit "$rc"
          cat out.txt
