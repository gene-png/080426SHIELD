# Gene's Context — 080426SHIELD

**Last updated:** 2026-08-20 (#98 merged — the structural audit gate, confirmed on GitHub; its own self-audit found 9 issues, 8 fixed, 1 accepted-and-pinned; critical catch in the PR body itself: the gate does not yet BLOCK a merge, only reports, until branch protection on main requires its status check — Gene asked for explicit go-ahead before configuring that setting, not yet done; W2 is WIP re-applied cleanly onto current main, 42 tests green, not yet a PR)

This file is owner-write-only. It exists so any session (mine or a relay) can pick up the real state of the project without reconstructing it from scattered chat history. Update it after every substantive decision. **This file lives in the repo, not on any one machine — a local computer restart does not affect it.**

## Branch / in flight

`main` has #78, #80, #76, #49, #50, #81, #82, #86, #88, #91, #93, #94, #95, #96, #97, #98 merged, all verified directly on GitHub. Only #29 remains open, permanently unmerged under D-054. #84, #85, #87, #89, #90, #92 filed and open. No W2 PR exists yet — checked GitHub directly, only #29 is open. W2 is described as re-applied cleanly onto current main after #98 merged, with 42 tests green; the UI half (citations_rejected/citations_normalized reaching the panel with real test coverage) is still to build, then the PR opens with the new audit-evidence gate satisfied and a real adversarial pass done before merge rather than after — the explicit lesson from #93/#94/#95.

## The audit gate is real but not yet enforced — one manual step needed from Gene

Read #98's PR body in full on GitHub. It shipped scripts/check_audit_evidence.py: fails a PR that touches code unless its body has an `## Adversarial audit` section with `Findings:` and `Disposition:` lines. Deterministic, stdlib-only, sub-second, no model in the loop.

Critical detail stated plainly in the PR body itself, not found by me independently: it does not block a merge on its own. The workflow job only reports; a docstring in the first draft claimed it blocked and that was wrong. It becomes an actual gate only once "Adversarial audit recorded" is registered as a required status check in main's branch protection settings on GitHub — something no file in the repo can configure, it requires a manual change in Settings → Branches. Until that one setting is added, a red X from this check is visible but does not stop a merge, which means the actual failure mode from #93/#94/#95 (silent skip) is only half-closed: it's now a visible skip rather than an invisible one, but still a skippable one.

I have browser access to Gene's GitHub session and could configure this directly, but it's a repository settings change, so I asked for explicit permission before touching it rather than doing it silently. Not yet actioned. This is the one real action item from this round — either Gene configures it, or authorizes me to.

## #98's own self-audit — 9 findings, 8 fixed, 1 accepted

Verified by reading the PR body directly. The adversarial-reviewer subagent was run against this exact change before it was opened as a PR, applying the new gate to itself. Findings, all confirmed except one downgraded to plausible-on-reachability: the first docstring claimed the gate blocks merges (wrong, per above) — the #72 pattern one level up, inside the very sentence claiming to avoid it; the CI trigger didn't include `edited` as a type, so pushing a compliant PR description after the fact never re-triggered the check, leaving a fixed PR stuck red; `**Findings:** none` (this repo's own dominant bold-label prose style) was rejected by the original regex, which only recognized the plain unbolded form; an empty changed-file list failed open with a positive "exempt" message; `main()` itself had zero test coverage, so inverting `return 1` to `return 0` would have left the whole suite green — the #72 shape recurring inside the tool built to catch #72; a 0-3 indent limit turned out to be accidental but load-bearing, since it's what stops the gate's own error message from satisfying its own check, now pinned by a test; `.claude/agents/adversarial-reviewer.md` was exempt as a `.md` file, meaning the reviewer's own definition could have been silently rewritten with no audit; and #94's excluded_count fix from two rounds ago was itself a half-fix, with four remaining copies of the false claim surviving, including in a test-file docstring that contradicted the correction 150 lines below it. The ninth finding (a fenced-code-block bypass) was accepted as within the gate's stated contract rather than fixed, and pinned by a test instead of patched.

Also corrected in this round: the #94 excluded_count fix itself was refined again — the prior round's correction was judged "over-alarming," since it implied a row is definitely dropped when the honest statement is narrower: the arithmetic cannot distinguish "nothing was excluded" from "a row was excluded and a different one produced two output items." Restated precisely in all five places it appeared. And D-051's pointer was fixed to name the correct file — the false reasoning it was correcting lives in DELIVERY_PLAN.md in two places, the living plan people actually work from, not in DECISIONS.md as the prior correction said.

## W2 status — re-applied cleanly, not yet a PR

Rather than untangle a rebase where WIP work overlapped with what #96 already shipped, the resolver was re-applied fresh onto current main after #98 merged, deliberately merging the two docstrings by hand instead of by conflict resolution. Landed so far, per the other session's own account, not yet independently verified since there's no PR to read: citations.py rewritten against an "exactly one candidate or give up" invariant; all four known-broken paths from issue #30's re-audit covered as named tests (nullable-vendor, name/vendor cross-ambiguity, substring-with-2+-matches, the finding-11 fold-asymmetry bug); `_client_capabilities` now carries vendor data so the ambiguity rules have something to check; W3's snapshot now records vendor too, so a post-approval vendor correction moves the allow-list the same deliberate way a name edit already does; the resolver is wired into run-AI in place of the old exact-match path, with 4 wiring tests; tri-state counts (confirmed/needs_review/rejected) are on the response with verbatim examples. 42 tests green total. One design note worth keeping: snapshots written before vendor tracking existed are read as unknown rather than as "no vendor," which makes the resolver more cautious on old data, never less — the correct direction for a fail-safe default.

Deliberately not folded into W2, per prior scoping and reconfirmed rather than silently expanded: 5.1's pending_review technique status (the resolver flags a citation as needing review; it does not yet change the coverage score — to be stated explicitly rather than let a surfaced-but-inert counter be mistaken for actual enforcement) and issue #33's three follow-ups, both still separately tracked.

## MVP tracking — status this round

Six of eleven items were done last round; this round adds nothing to that count on paper, and that's worth naming plainly rather than folding into a clean "on track" line. Item 5 (W2) is still not a merged PR. It's re-applied cleanly onto current main after conflicts (mostly "already landed via #96") and sits at 42 tests green, but re-applied-and-passing is not shipped, and this round's session time went mostly to fixing a live regression in already-merged W3 work and building the audit-evidence gate, not to forward progress on W2 itself. Legitimate work that needed doing, but the MVP clock did not move on item 5 this round even though the branch did.

## PR #96 — live regression fix, closes the approved-snapshot narrowing bug

W3's own approval snapshot (migration 0043, #95) could be silently narrowed after approval: override_security_classification and add_capability_components changed the live capability list without touching the frozen snapshot, and a falsy check (`if not snapshot`) treated an empty-but-recorded snapshot the same as "no snapshot," letting it silently re-derive from live data instead of surfacing staleness. Fix: explicit `is None` check, plus a new `approved_membership_stale` field computed on read so the client can see when the snapshot no longer matches current membership. Four new tests, red-on-revert verified. Confirmed merged directly on GitHub.

## PR #97 — mutation-sweep tool fix, closes the wrong-node-identity bug

The mutation-sweep tool built for W8a (#93) used `(type, lineno, col_offset)` as node identity, not unique across chained calls on one line — the exact #72 shape this tool exists to catch, recurring inside the catcher. Also fixed: the no-op guard compared raw `ast.unparse` output against raw file text, never equal even when semantically identical, so it always fired; and there was no green-baseline check, so any non-zero pytest exit was read as "mutant killed" regardless of whether the original suite passed. On attack.py, the no-op mutant count went from 8 before this fix to 0 after — real exposure avoided so far only because the nightly mutation-sweep workflow has never actually run (`gh run list --workflow=mutation-sweep.yml` came back empty). Confirmed merged directly on GitHub.

## PR #98 — the audit gate made structural, confirmed merged

`scripts/check_audit_evidence.py` fails a PR touching code unless the PR body contains an `## Adversarial audit` section with `Findings:` and `Disposition:` lines. Its own PR body carries a 9-finding self-audit: 8 confirmed and fixed, including that `main()` had zero test coverage before this PR (a return-code inversion would have passed silently) and that the bold-markdown `**Findings:**` style, this repo's dominant convention, wasn't originally recognized by the matching regex. One finding, a fenced-code-block bypass, was accepted as within contract and pinned by a test rather than fixed. D-051's original correction pointed at the wrong file (said "below," implying DECISIONS.md, when the false reasoning actually lived in DELIVERY_PLAN.md in two places) — now corrected with the right pointer. 21 tests including 4 new ones on `main()` itself; two specific fixes verified red-on-revert; 64 tests green across affected suites.

The critical piece, stated plainly because "merged" is easy to misread as "done": this script reports, it does not yet block. It requires "Adversarial audit recorded" to be registered as a required GitHub branch-protection status check on main, a manual setting no file in this repo can configure. Until that's set, a PR can still merge without the section present — the gate exists and works, but isn't load-bearing yet. I've asked Gene whether he wants me to configure that setting via the browser or whether he'll do it himself; not yet answered.

## D-054, PR #96, PR #97 — recap, see prior entries for full detail

D-054 (W2 vs #29 circularity): W2 cut fresh off main, not a rebase or merge of #29; #29 and issue #30 used as reference only; three open re-audit findings fixed as part of the rewrite; nullable-vendor default set to refuse-and-flag. PR #96 and #97 above are the first concrete payoff of that fresh-branch approach: both defects were caught by W2 work touching adjacent code, not by the original W3/W8a authors re-checking their own work.

## Session length — measured from git history (unchanged, see prior entries)

Still roughly 4 to 8 hours per session. Still needs revision once #84's W1-Risk fold-in, #90's build, #92's contract-test fix, W2, and item 3b are all landed and measured. This round adds two more merged PRs (#96, #97) to the sample without changing that range yet.

## Open decisions — NOT to be reconstructed from memory

Branch-protection required-status-check setting for "Adversarial audit recorded" — recommended, not yet configured, awaiting Gene's go-ahead on who does it. Local-device mirror of this file — delivered once to Gene's connected machine, not yet confirmed whether Gene wants it kept in sync after every future update. #90's build, #89's pin test, #92's contract-test fix — all still not built. #57, ServiceStatus.RELEASED (#62), W0's freeze shape — unchanged from prior entries. Nullable-vendor default under D-054 — adopted, not independently re-confirmed by Gene. Whether to parallelize item 6 with W2 — still undecided.

## Resolved as of this round

PR #96 confirmed merged: W3's approval snapshot can no longer be silently narrowed after approval; staleness now surfaces explicitly instead of silently re-deriving from live data. PR #97 confirmed merged: mutation-sweep tool's node-identity bug fixed, no-op guard fixed, green-baseline check added; real-world exposure on attack.py closed. PR #98 confirmed merged: the missed-audit-gate problem gets a structural fix, not just a documentation reminder; 9-finding self-audit on the tool itself before it shipped; critical caveat recorded that it reports but does not yet block, pending one manual branch-protection setting. W2 confirmed re-applied cleanly onto current main post-#98, 42 tests green, not yet a PR.

## Environment notes (standing)

Unchanged from prior entries: Postgres migrations clean through 0043, applied to dev Postgres and verified via alembic current. Provider live key: RESOLVED, reverted to fixture-by-default on purpose.

## Do not merge

PR #29 — unchanged from prior entries. Permanent do-not-merge under D-054; will be closed as superseded once W2 lands. Reference material only.

## Recurring defect shapes to watch for (CLAUDE.md)

A test that supplies its own expected value or precondition from the thing under test cannot fail — now 11 confirmed instances: the running count from prior rounds, plus this round's two new ones inside the very tools built to catch the pattern — the mutation-sweep tool's own no-op guard (#97) and `check_audit_evidence.py`'s own `main()` (#98), which had zero test coverage before this PR. A defect in one service or function exists in its twins until checked — unchanged from prior entries. A false branch dropping a record silently instead of surfacing it under a different reason — three recorded instances, none new this round. A status line that is wrong is worse than none — unchanged from prior entries. New this round: a mechanism that reports on a violation is not the same as a mechanism that blocks it, and treating "built" as "enforced" is its own recurring failure mode worth naming separately from the defect it was built to catch — #98 is a clean instance of getting this right, since the PR body states the gap explicitly, but the fact that it needed stating at all is the signal. Credential material never travels through a relay conversation, even when explicitly offered. Test behavior changes get called out explicitly in the test itself, never silently adjusted. A rule whose measured signal is a small fraction of its raw output should be narrowed rather than kept broad and ignored. A mutation-testing survivor is only meaningful relative to which tests were actually run against it — reinforced this round by #97's green-baseline fix. A migration's effect on existing rows is not uniform by default, and "old rows behave as before, new rows get the new behavior" has to be stated and tested as its own case.
